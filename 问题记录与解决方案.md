# 五子棋在线对战平台 - 问题记录与解决方案

> 项目版本: v5.2.0 (Vue 3 重构版)
> 记录日期: 2026-04-09 ~ 2026-04-10（本次修复 2 个问题）
> 项目: 五子棋在线对战平台（Vue 3 + TypeScript + Vite）

## 📚 相关文档

| 文档名称 | 说明 |
|---------|------|
| **项目上下文** | [AGENTS.md](./AGENTS.md) - 项目概述、技术栈、开发约定 |
| **代码分析** | [代码分析文档.md](./代码分析文档.md) - 详细代码实现分析、算法详解、对战模式逻辑 |
| **问题记录** | 本文档 - 已知问题、bug 修复记录、解决方案 |
| **迁移清单** | [frontend/MIGRATION_CHECKLIST.md](./frontend/MIGRATION_CHECKLIST.md) - 从 HTML 迁移到 Vue 3 的完整检查清单 |
| **项目说明** | [README.md](./README.md) - 项目简介、快速开始、部署指南 |

---

## 目录

1. [在线对战核心问题](#在线对战核心问题)
2. [功能完善问题](#功能完善问题)
3. [用户体验优化问题](#用户体验优化问题)
4. [数据一致性问题](#数据一致性问题)
5. [边界情况和异常处理](#边界情况和异常处理)

---

## 在线对战核心问题

### 问题1: GitHub Pages部署WebSocket连接错误

**描述**:
GitHub Pages部署环境下，前端没有使用正确的Railway WebSocket地址，显示连接错误。

**根本原因**:
前端代码中WebSocket地址判断逻辑不完整，未区分GitHub Pages环境。

**解决方案**:
在 `vite.config.ts` 中添加环境变量配置：
```typescript
define: {
  'import.meta.env.VITE_GITHUB_PAGES': process.env.GITHUB_PAGES
}
```

在前端代码中使用环境变量：
```javascript
const WS_SERVER = new URLSearchParams(window.location.search).get('ws') || 
  (import.meta.env.VITE_GITHUB_PAGES ? 'wss://gomoku-game-production.up.railway.app' : '/ws');
```

**涉及文件**:
- `frontend/vite.config.ts`
- `frontend/src/stores/websocket.ts`

---

### 问题2: 玩家B加入房间后无法进入游戏页面

**描述**:
玩家A创建房间后，玩家B能加入房间，但是无法进入棋盘页面。

**根本原因**:
`room_joined` 消息处理中缺少 `currentScreen.value = 'game'` 的设置。

**解决方案**:
在 `room_joined` 消息处理中添加：
```javascript
case 'room_joined':
  // ... 其他处理
  if (data.opponent) {
    // ... 游戏初始化
    currentScreen.value = 'game'; // 添加这一行
  } else {
    panels.value.waiting = true;
  }
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题3: 玩家名字显示为对象 { nickname: '玩家' }

**描述**:
在线对战中，己方名字显示为 `{ "nickname": "玩家" }` 而不是纯字符串。

**根本原因**:
`showCreateRoom` 函数参数类型错误，接收了字符串而不是对象。

**解决方案**:
修改函数签名和调用方式：
```javascript
// 修改前
showCreateRoom(nickname: string) {
  // ...
}

// 修改后
showCreateRoom(data: { nickname: string }) {
  // ...
}

// 调用时
showCreateRoom({ nickname: nickname });
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题4: 双方都显示自己是白棋

**描述**:
进入房间后，偶尔会出现玩家双方视角都显示自己是白棋、对方是黑棋。

**根本原因**:
1. 页面刷新时 `myColor` 默认值为 `1`（黑棋），导致加入者刷新后显示错误
2. 重连时 `rejoined` 消息没有恢复 `myColor`

**解决方案**:
1. 在 `savedRoomInfo` 中添加 `myColor` 字段
2. 在 `saveRoomInfo` 时保存 `myColor`
3. 在 `onMounted` 时从 localStorage 恢复 `myColor`
4. 在 `rejoined` 消息处理中恢复 `myColor`

```javascript
// 保存房间信息
function saveRoomInfo(roomCode, myName, myUserId, isHost, myColor) {
  savedRoomInfo.value = { roomCode, myName, myUserId, isHost, myColor };
  localStorage.setItem('gomoku-room', JSON.stringify({ 
    roomCode, myName, myUserId, isHost, myColor 
  }));
}

// 恢复房间信息
const savedRoom = wsStore.loadRoomInfo();
if (savedRoom) {
  gameStore.myColor = savedRoom.myColor;
  gameStore.isHost = savedRoom.isHost;
}

// 重连恢复
case 'rejoined':
  if (data.color !== undefined) {
    gameStore.myColor = data.color;
  }
```

**涉及文件**:
- `frontend/src/stores/websocket.ts`
- `frontend/src/App.vue`

---

### 问题5: 房主看不到比分

**描述**:
在在线对战中，房主视角看不见比分，非房主玩家能看到。

**根本原因**:
`opponent_joined` 消息中缺少 `matchMode` 字段，导致房主的 `matchMode` 被设置为默认值 `1`（单局模式），比分显示条件 `matchMode > 1` 不成立。

**解决方案**:
在 `opponent_joined` 消息中添加配置信息：
```javascript
send(room.players[0].ws, { 
  type: 'opponent_joined', 
  opponent: room.players[1],
  gameTime: room.gameTime,
  boardSize: room.boardSize,
  matchMode: room.matchMode, // 添加这一行
  undoLimit: room.undoLimit
});
```

**涉及文件**:
- `server/server.js`

---

### 问题6: 悔棋次数没有扣减

**描述**:
在线对战悔棋时，偶尔出现悔棋次数没有扣减的情况。

**根本原因**:
1. 后端 `handleUndoAccept` 函数中执行了 `moves--` 但忘记扣减 `undoLeft`
2. 前端在 `undo_accepted` 消息处理中自己扣减 `undoLeft`，但扣减的是接收消息方而不是发起悔棋请求方

**解决方案**:
1. 后端在 `handleUndoAccept` 中添加 `undoLeft--` 并发送最新状态
2. 前端从消息中同步 `undoLeft` 值

```javascript
// 后端
function handleUndoAccept(ws) {
  // ... 悔棋逻辑
  room.players[lastPlayerIndex].undoLeft--; // 扣减悔棋次数
  
  broadcastToRoom(roomCode, { 
    type: 'undo_accepted',
    players: room.players.map(p => p ? {
      time: p.time,
      moves: p.moves,
      undoLeft: p.undoLeft, // 发送最新悔棋次数
      color: p.color
    } : null)
  });
}

// 前端
case 'undo_accepted':
  gameStore.doUndo();
  if (data.players) {
    data.players.forEach((p: any) => {
      if (p) {
        gameStore.players[p.color].undoLeft = p.undoLeft; // 同步悔棋次数
      }
    });
  }
  showToast('悔棋成功');
```

**涉及文件**:
- `server/server.js`
- `frontend/src/App.vue`

---

### 问题7: 倒计时不同步

**描述**:
玩家A显示自己剩余3分钟，玩家B显示A剩余4分钟，时间不一致。

**根本原因**:
- 原设计由客户端各自维护倒计时，由于网络延迟和计算时间差异导致不同步
- 断线重连后没有正确恢复时间

**解决方案**:
1. 服务器每秒自动扣减当前玩家时间
2. 服务器每秒发送 `time_sync` 消息同步双方时间
3. 前端接收并更新显示

```javascript
// 服务器端
setInterval(() => {
  rooms.forEach((room, roomCode) => {
    if (room.status === 'playing') {
      const currentPlayerIndex = room.players.findIndex(p => p && p.color === room.currentPlayer);
      if (currentPlayerIndex !== -1) {
        room.players[currentPlayerIndex].time--;
        
        // 同步时间给所有客户端
        broadcastToRoom(roomCode, {
          type: 'time_sync',
          players: room.players.map(p => p ? {
            time: p.time,
            color: p.color
          } : null),
          currentPlayer: room.currentPlayer
        });
      }
    }
  });
}, 1000);

// 前端
case 'time_sync':
  if (data.players) {
    data.players.forEach((p: any) => {
      if (p) {
        gameStore.players[p.color].time = p.time;
      }
    });
  }
  if (data.currentPlayer) {
    gameStore.currentPlayer = data.currentPlayer;
  }
```

**涉及文件**:
- `server/server.js`
- `frontend/src/App.vue`

---

### 问题8: 悔棋逻辑错误

**描述**:
在线对战悔棋逻辑不正确，应该在自己落子后、对方落子前才可以发起悔棋。

**根本原因**:
原悔棋按钮启用条件过于宽松，允许任何时候悔棋。

**解决方案**:
修改悔棋按钮启用条件，只允许在自己落子后、对方落子前悔棋：

```javascript
const canUndo = computed(() => {
  // ... 其他模式判断
  
  if (props.gameState.gameMode === 'online') {
    const isMyTurn = currentPlayer === myColor;
    const lastMove = props.gameState.moveHistory[props.gameState.moveHistory.length - 1];
    const isMyLastMove = lastMove && lastMove.player === myColor;
    
    // 只能在自己落子后、对方落子前悔棋
    return isMyTurn && // 轮到对方
           isMyLastMove && // 最后一手是自己下的
           props.gameState.players[myColor].undoLeft > 0 &&
           props.gameState.moveHistory.length > 0;
  }
  
  return false;
});
```

**涉及文件**:
- `frontend/src/components/game/GameControls.vue`

---

### 问题9: rejoined消息缺少color字段

**描述**:
重连时服务器发送的 `rejoined` 消息中 `players` 数组缺少 `color` 字段，导致前端无法正确恢复玩家信息。

**根本原因**:
代码疏忽，忘记在 `players.map()` 中包含 `color` 字段。

**解决方案**:
在 `rejoined` 消息中添加 `color` 字段：

```javascript
send(ws, {
  type: 'rejoined',
  board: room.board,
  currentPlayer: room.currentPlayer,
  moves: room.moves,
  players: room.players.map(p => p ? { 
    name: p.name, 
    time: p.time, 
    moves: p.moves, 
    undoLeft: p.undoLeft,
    color: p.color // 添加这一行
  } : null),
  playerIndex: playerIndex,
  color: room.players[playerIndex].color
});
```

**涉及文件**:
- `server/server.js`

---

## 功能完善问题

### 问题10: 缺少URL参数自动加入房间功能

**描述**:
原HTML支持 `?room=xxx` 和 `?pwd=xxx` URL参数自动打开在线面板并加入房间，Vue项目未实现。

**解决方案**:
在 `onMounted` 中检查URL参数并自动加入：

```javascript
onMounted(() => {
  // ... 其他初始化
  
  // 检查URL参数，自动加入房间
  const urlParams = new URLSearchParams(window.location.search);
  const roomCode = urlParams.get('room');
  const password = urlParams.get('pwd');

  if (roomCode) {
    showOnlineSetup();
    gameStore.pendingRoomCode = roomCode;
    gameStore.myName = '玩家';

    if (password) {
      panels.value.password = true;
    } else {
      setTimeout(() => {
        doJoinRoom(roomCode, '玩家');
      }, 100);
    }
  }
});
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题11: 缺少复制链接功能

**描述**:
等待对手面板中缺少复制链接按钮，不方便邀请好友。

**解决方案**:
添加复制链接按钮和功能：

```javascript
function copyLink() {
  const url = window.location.origin + window.location.pathname + 
             '?room=' + roomCode.value;
  navigator.clipboard.writeText(url).then(() => {
    showToast('链接已复制到剪贴板');
  });
}
```

**涉及文件**:
- `frontend/src/components/online/WaitingPanel.vue`

---

### 问题12: 缺少自定义消息输入功能

**描述**:
只能发送预设的快捷消息，无法发送自定义内容。

**解决方案**:
添加自定义消息输入框：

```vue
<div v-if="showCustomInput" class="custom-input-container">
  <input v-model="customMessage" type="text" placeholder="输入自定义消息..." 
         maxlength="50" class="custom-input" @keyup.enter="sendCustomMessage" />
  <button class="btn-send" @click="sendCustomMessage" :disabled="!customMessage.trim()">
    发送
  </button>
  <button class="btn-close" @click="showCustomInput = false">✕</button>
</div>
```

**涉及文件**:
- `frontend/src/components/game/GameControls.vue`

---

### 问题13: 缺少AI思考状态图标

**描述**:
AI思考时没有视觉提示，用户体验不好。

**解决方案**:
在玩家信息中添加AI思考状态图标：

```vue
<div class="name">
  <span class="color-icon">{{ colorIcon }}</span>
  <span class="name-text">{{ displayName }}</span>
  <span v-if="isAIThinking" class="ai-thinking">🤔</span>
</div>
```

```css
.ai-thinking {
  font-size: 14px;
  margin-left: 4px;
  animation: thinking 1.5s ease-in-out infinite;
}

@keyframes thinking {
  0%, 100% { transform: rotate(-5deg); }
  50% { transform: rotate(5deg); }
}
```

**涉及文件**:
- `frontend/src/components/game/PlayerInfo.vue`

---

### 问题14: 缺少断线重连UI

**描述**:
断线时没有提示，用户不知道发生了什么。

**解决方案**:
添加断线重连UI：

```vue
<div v-if="showReconnect" class="reconnect-overlay">
  <div class="reconnect-content">
    <div class="reconnect-icon">🔌</div>
    <h3>连接断开</h3>
    <p>正在尝试重新连接... ({{ reconnectAttempts }}/20)</p>
    <div class="reconnect-spinner"></div>
  </div>
</div>
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题15: 查看棋谱后准备按钮无反应

**描述**:
对局结束、比赛结束后，点击查看棋谱后，下一局、再赛一轮按钮点击无反应。

**根本原因**:
GameControls 组件中缺少 `@ready` 事件绑定。

**解决方案**:
添加 `@ready` 事件绑定：

```vue
<GameControls
  :game-state="gameStateForControls"
  :show-move-numbers="showMoveNumbers"
  :show-emoji-popup="showEmojiPopup"
  :is-ready="isReady"
  @ready="handleReady"
  @undo="handleUndo"
  @surrender="handleSurrender"
  @play-again="handlePlayAgain"
  @toggle-numbers="gameStore.showMoveNumbers = !gameStore.showMoveNumbers"
  @export-game="exportGame"
  @exit-room="handleExitRoom"
  @quick-msg="handleQuickMsg"
  @toggle-emoji="showEmojiPopup = !showEmojiPopup"
  @send-emoji="handleSendEmoji"
  @send-custom="handleSendCustom"
/>
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题16: 查看棋谱页面消息框变窄

**描述**:
查看棋谱时，消息框宽度比对战中窄。

**根本原因**:
消息框缺少明确的宽度设置。

**解决方案**:
添加 `width: 100%`：

```css
.chat-messages {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin: 4px 0;
  padding: 6px 10px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 8px;
  height: 12vh;
  max-height: 120px;
  overflow-y: auto;
  min-height: 80px;
  width: 100%; /* 添加这一行 */
}
```

**涉及文件**:
- `frontend/src/components/game/GameControls.vue`

---

## 用户体验优化问题

### 问题17: 消息框高度问题

**描述**:
消息框高度不固定，发送消息后消息框一直增长，影响体验。

**解决方案**:
使用 vh 单位固定高度，并支持上下滚动：

```css
.chat-messages {
  height: 12vh;
  max-height: 120px;
  min-height: 80px;
  overflow-y: auto;
}

@media (max-width: 767px) {
  .chat-messages {
    height: 15vh;
    min-height: 80px;
    max-height: 100px;
  }
}
```

**涉及文件**:
- `frontend/src/components/game/GameControls.vue`

---

### 问题18: 双方信息离边界太近

**描述**:
双方玩家信息贴近边界，不方便看清。

**解决方案**:
增加 padding 和 margin：

```css
.player-info {
  background: rgba(255, 255, 255, 0.08);
  padding: 8px 16px;
  border-radius: 10px;
  margin: 0 6px;
}

@media (max-width: 767px) {
  .player-info {
    padding: 6px 12px;
    margin: 0 4px;
  }
}

@media (min-width: 768px) {
  .player-info {
    padding: 10px 18px;
    margin: 0 8px;
  }
}
```

**涉及文件**:
- `frontend/src/components/game/PlayerInfo.vue`

---

### 问题19: 快捷消息位置不合理

**描述**:
快捷消息在消息框下方，不方便输入。

**解决方案**:
将快捷消息组移到消息框上方：

```vue
<!-- 聊天消息显示 -->
<div class="chat-messages">...</div>

<!-- 快捷消息组 -->
<div class="chat-bar">
  <button class="quick-msg">你好！</button>
  <button class="quick-msg">请稍等</button>
  <button class="quick-msg">好棋！</button>
  <button class="quick-msg">GG</button>
  <button class="emoji-btn">😊</button>
</div>
```

**涉及文件**:
- `frontend/src/components/game/GameControls.vue`

---

### 问题20: 缺少页面缩放限制

**描述**:
用户可以缩放页面，导致布局混乱。

**解决方案**:
在 HTML 中添加缩放限制：

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
```

**涉及文件**:
- `frontend/index.html`

---

### 问题21: 移动端布局优化

**描述**:
移动端布局不够优化，用户体验不好。

**解决方案**:
1. 使用 vh/vw 单位
2. 优化按钮间距
3. 调整字体大小
4. 优化消息框高度

**涉及文件**:
- `frontend/src/App.vue`
- `frontend/src/components/game/GameControls.vue`
- `frontend/src/components/game/PlayerInfo.vue`

---

## 数据一致性问题

### 问题22: 前后端状态不一致

**描述**:
前后端状态管理方式不同，容易导致数据不一致。

**根本原因**:
- 后端使用位置索引：`players[0]`（房主）、`players[1]`（加入者）
- 前端使用颜色索引：`players[1]`（黑棋）、`players[2]`（白棋）

**解决方案**:
1. 统一使用 `color` 字段进行映射
2. 所有状态更新由服务器发起
3. 前端只负责显示，不进行状态计算

**核心原则**:
```javascript
// 后端
room.players[playerIndex].color = 1; // 或 2

// 前端
gameStore.players[p.color] = p; // p.color 是 1 或 2
```

**涉及文件**:
- `server/server.js`
- `frontend/src/App.vue`
- `frontend/src/stores/game.ts`

---

### 问题23: LocalStorage持久化不完整

**描述**:
页面刷新后，部分状态丢失。

**解决方案**:
扩展 LocalStorage 存储内容：

```javascript
// 保存
function saveRoomInfo(roomCode, myName, myUserId, isHost, myColor) {
  savedRoomInfo.value = { 
    roomCode, 
    myName, 
    myUserId, 
    isHost, 
    myColor 
  };
  localStorage.setItem('gomoku-room', JSON.stringify({ 
    roomCode, 
    myName, 
    myUserId, 
    isHost, 
    myColor 
  }));
}

// 恢复
const savedRoom = wsStore.loadRoomInfo();
if (savedRoom) {
  gameStore.roomCode = savedRoom.roomCode;
  gameStore.myName = savedRoom.myName;
  gameStore.myUserId = savedRoom.myUserId;
  gameStore.isHost = savedRoom.isHost;
  gameStore.myColor = savedRoom.myColor; // 重要！
}
```

**涉及文件**:
- `frontend/src/stores/websocket.ts`
- `frontend/src/App.vue`

---

## 边界情况和异常处理

### 问题24: 房主强制黑棋

**描述**:
房主创建房间时颜色可能不确定。

**解决方案**:
房主创建房间时强制设置为黑棋：

```javascript
case 'room_created':
  gameStore.roomCode = data.roomCode;
  gameStore.myColor = 1; // 房主是黑棋
  gameStore.isHost = true;
  gameStore.myUserId = data.userId;
  wsStore.saveRoomInfo(data.roomCode, gameStore.myName, data.userId, true, 1);
  break;
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题25: WebSocket消息处理不完整

**描述**:
部分 WebSocket 消息处理不完整，导致状态不同步。

**解决方案**:
确保所有消息都完整处理：

```javascript
case 'opponent_joined':
  const opponentName = data.opponent?.name || data.opponentName || '对手';
  gameStore.opponentName = opponentName;
  panels.value.waiting = false;
  gameStore.undoLimit = data.undoLimit || 3;
  gameStore.startGame('online', {
    player1: gameStore.myColor === 1 ? gameStore.myName : opponentName,
    player2: gameStore.myColor === 2 ? gameStore.myName : opponentName,
    time: data.time || 300,
    size: data.size || 15,
    matchMode: (data.matchMode || 1) as MatchMode // 重要！
  });
  currentScreen.value = 'game';
  break;
```

**涉及文件**:
- `frontend/src/App.vue`

---

### 问题26: 比分显示逻辑错误

**描述**:
比分显示没有按视角正确显示。

**解决方案**:
根据 `isHost` 正确显示比分：

```javascript
const matchWins = computed(() => {
  // 按视角显示比分
  // matchWins[1] = 房主得分, matchWins[2] = 加入者得分
  if (gameStore.isHost) {
    return { myScore: gameStore.matchWins[1], opponentScore: gameStore.matchWins[2] };
  } else {
    return { myScore: gameStore.matchWins[2], opponentScore: gameStore.matchWins[1] };
  }
});
```

**涉及文件**:
- `frontend/src/App.vue`

---

## 总结

### 问题分类统计

| 分类 | 问题数 | 严重程度 |
|------|--------|---------|
| 在线对战核心问题 | 9 | 高 |
| 功能完善问题 | 7 | 中 |
| 用户体验优化问题 | 5 | 中 |
| 数据一致性问题 | 2 | 高 |
| 边界情况和异常处理 | 3 | 中 |
| **总计** | **26** | - |

### 关键修复点

1. ✅ WebSocket 连接和消息处理
2. ✅ 颜色状态管理和恢复
3. ✅ 倒计时同步机制
4. ✅ 悔棋次数扣减
5. ✅ 前后端状态一致性
6. ✅ 断线重连和状态恢复
7. ✅ 用户体验优化

### 最佳实践

1. **单向数据流**：所有游戏状态由服务器维护，前端只负责显示
2. **状态恢复机制**：支持断线重连和页面刷新后恢复完整状态
3. **错误处理**：WebSocket连接失败自动重连，超时判负
4. **用户体验**：Toast提示、动画效果、响应式设计
5. **代码质量**：TypeScript类型安全、组件化、可维护性

---

## 2026-04-10 更新记录

### 问题10: Docker部署WebSocket消息格式错误

**描述**:
Docker部署环境下，通过IP访问时，玩家加入房间显示"消息格式错误"，但玩家能进入房间并发送消息，房主无法进入页面但能收到消息。

**根本原因**:
`handleJoinRoom` 函数中发送包含 `ws` 属性的玩家对象，导致 `JSON.stringify` 遇到循环引用失败：
```javascript
// server.js
send(room.players[0].ws, {
  type: 'opponent_joined',
  opponent: room.players[1],  // ❌ 包含 ws 属性
});
```

**解决方案**:
只发送必要的属性，避免包含 `ws`：
```javascript
send(room.players[0].ws, {
  type: 'opponent_joined',
  opponent: room.players[1] ? {
    name: room.players[1].name,
    time: room.players[1].time,
    moves: room.players[1].moves,
    undoLeft: room.players[1].undoLeft,
    color: room.players[1].color
  } : null,
});
```

**影响范围**:
- `handleJoinRoom` 函数（两处）
- 参考 `handleRejoinRoom` 的正确实现

**验证方法**:
查看服务器日志中的异常信息：
```
TypeError: Converting circular structure to JSON
at JSON.stringify
at send
at handleJoinRoom
```

---

### 问题11: Docker WebSocket代理配置缺失

**描述**:
Docker部署时，IP访问无法连接WebSocket，需要手动添加 `?ws=ws://localhost:8080` 参数。

**根本原因**:
Vite开发服务器没有配置WebSocket代理。

**解决方案**:
在 `vite.config.ts` 中添加代理配置：
```typescript
export default defineConfig({
  server: {
    proxy: {
      '/ws': {
        target: 'ws://localhost:8080',
        ws: true,
        changeOrigin: true
      }
    }
  }
});
```

**效果**:
- 开发模式下自动代理 `/ws` 到本地后端
- 无需手动添加参数
- 生产环境（Docker）使用nginx代理

---

### 问题12: Emoji消息重复显示

**描述**:
发送emoji时，自己的消息同时出现在左边（对手）和右边（自己），但发送文本消息时正常。

**根本原因**:
1. `handleSendEmoji` 中手动添加了一次消息
2. 服务器广播emoji给所有人（包括发送者）
3. 前端收到后又添加了一次
4. 导致重复显示

**解决方案**:
```javascript
// 移除手动添加
function handleSendEmoji(emoji: string) {
  wsStore.sendEmoji(emoji);  // 只发送，不手动添加
}

// 前端根据 playerColor 判断
case 'emoji':
  const isMyEmoji = data.playerColor === gameStore.myColor;
  gameStore.addChatMessage(data.emoji || '', isMyEmoji ? 'me' : 'opponent', 'emoji');
  break;
```

**关键要点**:
- emoji消息广播给所有人（包括发送者）
- 快捷消息只发送给对手
- 根据消息类型选择正确的处理方式

---

### 问题13: 房间销毁逻辑不一致

**描述**:
加入者离开房间时，房间也被删除。应该只有房主离开时才删除房间。

**根本原因**:
```javascript
// ❌ 错误：任何玩家离开都删除房间
function handleLeaveRoom(ws) {
  rooms.delete(roomCode);
}
```

**解决方案**:
```javascript
// ✅ 正确：只有房主离开时删除房间
function handleLeaveRoom(ws) {
  const isHost = userId === room.hostId;
  
  if (isHost) {
    // 房主离开，删除房间
    rooms.delete(roomCode);
    broadcastToRoom(roomCode, { type: 'opponent_left' });
  } else {
    // 加入者离开，只通知房主
    const host = room.players.find(p => p && p.id === room.hostId);
    if (host && host.ws) {
      send(host.ws, { type: 'opponent_left' });
    }
  }
}
```

**影响范围**:
- 在线对战房间管理
- 房主/加入者不同的行为

---

### 问题14: 重连识别使用玩家名称

**描述**:
如果两个玩家使用相同的昵称（如"玩家"），重连时会连接到错误的玩家。

**根本原因**:
```javascript
// ❌ 错误：使用 playerName 匹配
for (let i = 0; i < room.players.length; i++) {
  if (room.players[i].name === playerName) {
    playerIndex = i;
    break;
  }
}
```

**解决方案**:
```javascript
// ✅ 正确：使用 userId 匹配
for (let i = 0; i < room.players.length; i++) {
  if (room.players[i].id === userId) {
    playerIndex = i;
    break;
  }
}
```

**前后端统一**:
- 前端发送 `rejoin_room` 时包含 `userId`
- 服务器使用 `userId` 匹配玩家

---

### 问题15: 页面刷新重连缺少gameMode

**描述**:
页面刷新后，`gameMode.value` 被重置，导致重连检查条件失败。

**根本原因**:
```javascript
// ❌ 缺少 gameMode
saveRoomInfo(roomCode, myName, myUserId, isHost, myColor);

// onMounted 检查
if (savedRoom && gameMode.value === 'online') {  // gameMode 已被重置
  wsStore.autoReconnect();
}
```

**解决方案**:
```javascript
// ✅ 添加 gameMode 参数
function saveRoomInfo(roomCode, myName, myUserId, isHost, myColor, gameMode) {
  localStorage.setItem('gomoku-room', JSON.stringify({
    roomCode, myName, myUserId, isHost, myColor, gameMode
  }));
}

// ✅ 恢复 gameMode
onMounted(() => {
  const savedRoom = wsStore.loadRoomInfo();
  if (savedRoom && savedRoom.gameMode === 'online') {
    gameMode.value = 'online';  // 恢复游戏模式
    wsStore.autoReconnect();
  }
});
```

**向后兼容性**:
- 旧数据没有 `gameMode` 字段会自然失败（安全）
- 新数据正确恢复

---

### 问题16: 重连超时后定时器未清理

**描述**:
重连超时处理后，`room.reconnectTimer` 未被清理，可能导致状态不一致。

**解决方案**:
```javascript
room.reconnectTimer = setTimeout(() => {
  // ... 超时处理逻辑
  room.reconnectTimer = null;  // ✅ 清理定时器
}, RECONNECT_TIMEOUT);
```

**效果**:
- 确保状态一致性
- 避免定时器泄漏

---

### 问题17: 重连失败缺少用户提示

**描述**:
重连次数用完后，只在控制台输出错误，用户不知道发生了什么。

**解决方案**:
```javascript
// 触发自定义事件
window.dispatchEvent(new CustomEvent('websocket-reconnect-failed'));

// 监听事件
window.addEventListener('websocket-reconnect-failed', () => {
  showToast('连接已断开，请刷新页面重新连接');
  showReconnect.value = false;
});
```

**效果**:
- 显示友好的错误提示
- 引导用户刷新页面

---

### 问题18: 房间不存在错误处理不完整

**描述**:
玩家刷新后重连，如果房间已被删除，会收到"房间不存在"错误，但localStorage未清理。

**解决方案**:
```javascript
case 'error':
  if (data.message?.includes('房间不存在')) {
    wsStore.clearRoomInfo();
    // 如果在游戏中，返回菜单
    if (gameMode.value === 'online' && gameStore.isPlaying) {
      gameStore.backToMenu();
      currentScreen.value = 'menu';
      showReconnect.value = false;
    }
  }
  break;
```

**效果**:
- 自动清理localStorage
- 返回菜单
- 显示错误提示

---

### 问题19: 悔棋同意弹出框样式不统一

**描述**:
认输确认弹框和退出确认弹框使用浏览器原生 `confirm()`，样式与其他弹框不一致。

**解决方案**:
创建统一样式的组件：
- `SurrenderConfirmModal.vue` - 认输确认弹框
- `ExitConfirmModal.vue` - 退出确认弹框

参考 `UndoRequestModal.vue` 的设计：
- 深色背景 `#2c3e50`
- 半透明黑色遮罩 `rgba(0, 0, 0, 0.9)`
- 统一的按钮样式和布局

---

### 问题20: 复制房间链接兼容性问题

**描述**:
Docker部署后通过IP访问时，复制房间链接失败，显示"消息格式错误"。

**根本原因**:
Clipboard API 在非安全上下文（HTTP + 非localhost）中被禁用。

**解决方案**:
```javascript
async function copyLink() {
  const url = getRoomUrl();
  
  try {
    // 方案 A: 现代 API (HTTPS 环境)
    if (navigator.clipboard && window.isSecureContext) {
      await navigator.clipboard.writeText(url);
      copySuccess.value = true;
      return;
    }

    // 方案 B: 降级方案（创建临时 textarea）
    const textArea = document.createElement('textarea');
    textArea.value = url;
    textArea.style.position = 'fixed';
    textArea.style.left = '-9999px';
    textArea.style.top = '-9999px';
    textArea.style.opacity = '0';
    textArea.setAttribute('readonly', '');
    
    document.body.appendChild(textArea);
    textArea.focus();
    textArea.select();
    
    const success = document.execCommand('copy');
    document.body.removeChild(textArea);
    
    if (success) {
      copySuccess.value = true;
    }
  } catch (error) {
    console.error('复制失败:', error);
  }
}
```

**关键要点**:
- 先检查条件，避免访问 `undefined` 对象
- 降级方案作为正常流程，不在 `.catch()` 中
- 检查 `execCommand` 返回值

---

## 问题解决方案总结

### 核心问题修复

1. ✅ WebSocket循环引用 - 避免发送 `ws` 属性
2. ✅ WebSocket代理配置 - Vite/nginx 代理
3. ✅ Emoji重复显示 - 根据 `playerColor` 判断
4. ✅ 房间销毁逻辑 - 只有房主离开时删除
5. ✅ 重连识别 - 使用 `userId` 而不是 `playerName`
6. ✅ 页面刷新重连 - 保存和恢复 `gameMode`
7. ✅ 重连超时处理 - 清理定时器
8. ✅ 重连失败提示 - 友好的用户提示
9. ✅ 房间不存在处理 - 清理localStorage并返回菜单

### 用户体验优化

10. ✅ UI组件统一样式 - 认输/退出确认弹框
11. ✅ 复制链接兼容性 - Clipboard API 降级方案

### 数据一致性

12. ✅ 前后端状态统一 - userId 匹配
13. ✅ localStorage 完整性 - 保存 gameMode

### 边界情况处理

14. ✅ 重连超时后重连 - 状态正确恢复
15. ✅ 房间已删除重连 - 自动返回菜单

---

### 问题21: 加入者退出房间后棋盘初始化错误

**描述**:
玩家B加入房间后落子，然后退出房间。玩家C加入房间后尝试落子时，服务器报错：
```
TypeError: Cannot read properties of undefined (reading '10') at handlePlacePiece
```

**根本原因**:
加入者退出房间时，清空棋盘的代码不正确：
```javascript
// ❌ 错误：只是设置为空数组
room.board = [];

// 问题：room.board[row] 是 undefined
// handlePlacePiece 访问 room.board[row][col] 时报错
if (room.board[row][col] !== 0) { ... }
```

**解决方案**:
使用正确的棋盘初始化逻辑：
```javascript
// ✅ 正确：正确初始化二维数组
room.board = [];
for (let i = 0; i < room.boardSize; i++) {
  room.board[i] = [];
  for (let j = 0; j < room.boardSize; j++) room.board[i][j] = 0;
}
```

**完整修复流程：**
```javascript
// 清理玩家位置，允许新玩家加入
const playerIndex = room.players.findIndex(p => p && p.id === userId);
if (playerIndex !== -1) {
  room.players[playerIndex] = null;
  
  // 正确初始化棋盘
  room.board = [];
  for (let i = 0; i < room.boardSize; i++) {
    room.board[i] = [];
    for (let j = 0; j < room.boardSize; j++) room.board[i][j] = 0;
  }
  
  // 清空游戏状态
  room.currentPlayer = 1;
  room.moves = [];
  room.status = 'waiting';
  room.lastActivityAt = Date.now();
  
  // 重置房主状态
  if (host) {
    host.time = room.gameTime;
    host.moves = 0;
    host.undoLeft = room.undoLimit;
  }
}
```

**影响范围**:
- `handleLeaveRoom` 函数
- `handlePlacePiece` 函数
- 前端 `opponent_left` 消息处理

**验证方法**:
1. 玩家B加入房间并落子
2. 玩家B退出房间
3. 玩家C加入房间
4. 玩家C尝试落子（应该成功，不报错）

**关键要点**:
- JavaScript 二维数组必须正确初始化
- `[]` 只是创建空数组，不等于二维空数组
- 必须先创建外层数组，再为每一行创建内层数组
- 与创建房间时的棋盘初始化逻辑保持一致

---

### 问题22: 加入者退出房间后游戏状态未完全重置

**描述**:
玩家B退出房间后，玩家C加入房间时发现：
- 房主倒计时不对
- 玩家C显示"思考中"（不正确）
- 房主这边没有加入提醒

**根本原因**:
加入者退出房间时，只清理了玩家位置，但没有清空游戏状态。

**解决方案**:
**服务器端：清空所有游戏状态**
```javascript
// 清空游戏状态，重置为初始状态
room.board = [];  // 正确初始化二维数组
for (let i = 0; i < room.boardSize; i++) {
  room.board[i] = [];
  for (let j = 0; j < room.boardSize; j++) room.board[i][j] = 0;
}
room.currentPlayer = 1;  // 重置为房主先手
room.moves = [];  // 清空走棋记录
room.status = 'waiting';  // 重置房间状态为等待

// 重置房主状态
host.time = room.gameTime;
host.moves = 0;
host.undoLeft = room.undoLimit;
```

**前端：收到消息时清空游戏状态**
```javascript
case 'opponent_left':
  showToast('对手已离开房间');
  if (gameMode.value === 'online' && gameStore.isPlaying) {
    gameStore.board = gameStore.createEmptyBoard();
    gameStore.currentPlayer = 1;
    gameStore.moveHistory = [];
    gameStore.players[1].time = gameStore.players[1].time;
    gameStore.players[1].moves = 0;
    gameStore.players[1].undoLeft = gameStore.undoLimit;
    gameStore.isPlaying = false;
    panels.value.waiting = true;
  }
  break;
```

**完整的房间生命周期（修复后）：**
1. 房主创建房间 → `status = 'waiting'`，棋盘空
2. 玩家B加入 → `status = 'playing'`，游戏开始
3. 玩家B落子后退出 → 棋盘清空，状态重置为 `waiting`
4. 玩家C加入 → 棋盘正确初始化，游戏正常开始

**关键要点**:
- 加入者退出时，需要完全重置游戏状态
- 包括棋盘、走棋记录、当前玩家、计时器等
- 房主状态也需要重置（时间、走棋数、悔棋次数）
- 前端收到消息时同步清空状态

---

## 问题解决方案总结（更新）

### 核心问题修复（11个）

1. ✅ WebSocket循环引用 - 避免发送 `ws` 属性
2. ✅ WebSocket代理配置 - Vite/nginx 代理
3. ✅ Emoji重复显示 - 根据 `playerColor` 判断
4. ✅ 房间销毁逻辑 - 只有房主离开时删除
5. ✅ 重连识别 - 使用 `userId` 而不是 `playerName`
6. ✅ 页面刷新重连 - 保存和恢复 `gameMode`
7. ✅ 重连超时处理 - 清理定时器
8. ✅ 重连失败提示 - 友好的用户提示
9. ✅ 房间不存在处理 - 清理localStorage并返回菜单
10. ✅ 棋盘初始化错误 - 正确初始化二维数组
11. ✅ 游戏状态未完全重置 - 完全清空所有游戏状态

### 用户体验优化（2个）

12. ✅ UI组件统一样式 - 认输/退出确认弹框
13. ✅ 复制链接兼容性 - Clipboard API 降级方案

### 数据一致性（2个）

14. ✅ 前后端状态统一 - userId 匹配
15. ✅ localStorage 完整性 - 保存 gameMode

### 边界情况处理（2个）

16. ✅ 重连超时后重连 - 状态正确恢复
17. ✅ 房间已删除重连 - 自动返回菜单

---

---

**文档版本**: v2.1  
**最后更新**: 2026-04-10

## 2026-04-10 新增问题

### 问题23: 对手退出后房主视角状态未更新

**描述**:
玩家B退出房间后，房主A视角看到的状态没有更新，仍然显示旧的棋盘和时间。

**根本原因**:
1. 前端 `opponent_left` 消息处理中使用了不存在的 `gameStore.createEmptyBoard()` 方法
2. 服务器发送的 `opponent_left` 消息缺少房主的最新状态信息

**解决方案**:
1. 修复前端方法调用：使用 `gameStore.initBoard(gameStore.boardSize)` 替代 `gameStore.createEmptyBoard()`
2. 服务器端在 `opponent_left` 消息中添加房主的最新状态信息
3. 前端从服务器消息中同步房主状态，而不是自己重置

**涉及文件**:
- `frontend/src/App.vue`
- `server/server.js`

**验证方法**:
1. 房主A创建房间
2. 玩家B加入房间并下几颗棋
3. 玩家B退出房间
4. 房主A看到"👋 对手已离开房间"弹窗
5. 棋盘清空，时间恢复初始值

---

### 问题24: 对手退出提示不够明显

**描述**:
对手退出房间时，房主只看到一个小的 Toast 提示，不够明显，用户容易忽略。

**根本原因**:
使用简单的 Toast 提示，没有专门的弹窗面板。

**解决方案**:
创建 `OpponentLeftPanel.vue` 组件，与 `WaitingPanel.vue` 类似的弹窗设计：
- 显示"👋 对手已离开房间"标题
- 显示房间码和密码
- 提示"游戏已重置，等待新对手加入..."
- 提供复制链接和取消按钮

**涉及文件**:
- `frontend/src/components/online/OpponentLeftPanel.vue` (新建)
- `frontend/src/App.vue`

---

### 问题25: 重连后无法进入游戏页面

**描述**:
玩家B刷新浏览器后，房主A能收到玩家B重连消息，但是玩家B无法进入对局页面，也无法创建房间或查看房间列表。

**根本原因**:
`rejoined` 消息处理中缺少关键状态设置：
- 没有设置 `gameMode.value = 'online'`
- 没有设置 `gameStore.isPlaying = true`
- 没有设置 `currentScreen.value = 'game'`

**解决方案**:
在 `rejoined` 消息处理中添加关键状态设置，确保重连后能正确进入游戏页面。

**涉及文件**:
- `frontend/src/App.vue`
- `server/server.js` (添加 id 字段)

---

### 问题26: 前端开发服务器频繁崩溃

**描述**:
开发模式下，前端开发服务器（Vite）频繁崩溃，错误信息为 `ECONNRESET`。

**根本原因**:
WebSocket 代理配置过于复杂，频繁的连接/断开操作导致代理层错误。

**解决方案**:
1. 简化 WebSocket 代理配置，只保留基本的 `ws: true` 选项
2. 移除可能导致崩溃的 `configure` 回调
3. 使用相对路径 `/ws`，不写死 localhost

**涉及文件**:
- `frontend/vite.config.ts`
- `frontend/src/stores/websocket.ts`

---

### 问题27: 移动端设备无法创建房间

**描述**:
移动端设备在退出房间后，无法创建房间、无法查看房间列表，显示"连接服务器失败"。

**根本原因**:
LocalStorage 中保存了旧的房间信息，导致：
1. 页面加载时自动尝试重连
2. 重连失败后没有清理 LocalStorage
3. 用户再尝试操作时，状态被卡住

**解决方案**:
1. 自动重连失败时清理 LocalStorage
2. 优化重连失败提示，清理游戏状态
3. 在创建房间、获取房间列表、加入房间之前清理旧的房间信息

**涉及文件**:
- `frontend/src/stores/websocket.ts`
- `frontend/src/App.vue`

**验证方法**:
1. 移动端设备刷新页面
2. 看到提示"连接已断开，请刷新页面重新连接"
3. 点击"在线对战"
4. 创建房间 → 成功
5. 查看房间列表 → 成功

---

## 问题解决方案总结（最终版本）

### 核心问题修复（17个）

1. ✅ WebSocket循环引用 - 避免发送 `ws` 属性
2. ✅ WebSocket代理配置 - Vite/nginx 代理
3. ✅ Emoji重复显示 - 根据 `playerColor` 判断
4. ✅ 房间销毁逻辑 - 只有房主离开时删除
5. ✅ 重连识别 - 使用 `userId` 而不是 `playerName`
6. ✅ 页面刷新重连 - 保存和恢复 `gameMode`
7. ✅ 重连超时处理 - 清理定时器
8. ✅ 重连失败提示 - 友好的用户提示
9. ✅ 房间不存在处理 - 清理localStorage并返回菜单
10. ✅ 棋盘初始化错误 - 正确初始化二维数组
11. ✅ 游戏状态未完全重置 - 完全清空所有游戏状态
12. ✅ createEmptyBoard方法不存在 - 使用正确的 initBoard 方法
13. ✅ 重连后无法进入游戏 - 添加关键状态设置
14. ✅ 前端开发服务器崩溃 - 简化代理配置
15. ✅ 移动端设备无法创建房间 - 清理 LocalStorage
16. ✅ 对手退出提示不明显 - 创建 OpponentLeftPanel 组件
17. ✅ 对手退出后状态未更新 - 同步服务器状态

### 用户体验优化（3个）

18. ✅ UI组件统一样式 - 认输/退出确认弹框
19. ✅ 复制链接兼容性 - Clipboard API 降级方案
20. ✅ 对手退出弹窗 - 明显的提示面板

### 数据一致性（3个）

21. ✅ 前后端状态统一 - userId 匹配
22. ✅ localStorage 完整性 - 保存 gameMode
23. ✅ 房主状态同步 - 从服务器消息中同步

### 边界情况处理（3个）

24. ✅ 重连超时后重连 - 状态正确恢复
25. ✅ 房间已删除重连 - 自动返回菜单
26. ✅ 重连失败后清理 - 避免 LocalStorage 污染

---

### 问题28: 房主/玩家刷新后无法进入对局页面

**描述**:
房主或玩家刷新浏览器后，虽然能发送 `rejoin_room` 消息并收到服务器的 `rejoined` 响应，但无法进入对局页面。浏览器控制台没有显示 `[rejoined] 收到重连成功消息` 的日志。

**根本原因**:
在 `onMounted` 钩子中，当检测到保存的房间信息并尝试自动重连时：
1. ✅ 恢复了游戏状态（`gameMode`、`myColor`、`isHost` 等）
2. ✅ 调用了 `wsStore.autoReconnect()` 发送重连请求
3. ❌ **但忘记调用 `setupWSMessageHandler()` 注册消息处理器**

这导致：
- WebSocket 连接成功
- `rejoin_room` 消息成功发送
- 服务器正确处理并发送了 `rejoined` 响应
- **但前端没有消息处理器来接收这个响应**

**解决方案**:
在 `frontend/src/App.vue` 的 `onMounted` 中，在调用 `wsStore.autoReconnect()` 之前，先调用 `setupWSMessageHandler()`：

```javascript
onMounted(() => {
  // ... 其他初始化

  const savedRoom = wsStore.loadRoomInfo();
  if (savedRoom && savedRoom.gameMode === 'online') {
    console.log('[App onMounted] 开始自动重连，房间信息:', savedRoom);

    // 重要：先注册消息处理器，否则无法接收到 rejoined 响应
    setupWSMessageHandler();

    // 恢复游戏模式
    gameStore.gameMode = 'online';
    // ... 其他恢复逻辑

    // 尝试自动重连
    wsStore.autoReconnect();
  }
});
```

**涉及文件**:
- `frontend/src/App.vue`

**验证方法**:
1. 房主创建房间，玩家加入
2. 房主刷新页面 → 成功进入对局页面
3. 玩家刷新页面 → 成功进入对局页面
4. 双方都刷新 → 都能成功进入对局页面

---

### 问题29: 一方刷新后，对方退出房间无法创建房间

**描述**:
一方刷新后，对方退出房间，此时无法创建新房间、无法查看房间列表。

**根本原因**:
1. **房主离开时，服务器发送的 `opponent_left` 消息缺少 `players` 字段**
   - 加入者离开时有 `players` 字段
   - 房主离开时没有 `players` 字段
   - 导致前端无法正确同步状态

2. **前端收到 `opponent_left` 消息后，没有清理 LocalStorage**
   - 当对方离开时，应该清理房间信息
   - 否则用户再次尝试操作时，会被旧的房间信息卡住

3. **前端的 `opponent_left` 处理只在 `isPlaying` 时生效**
   - 如果用户刷新后（此时 `isPlaying = false`），对手退出
   - 前端不会处理 `opponent_left` 消息
   - 导致状态不同步

**解决方案**:

**1. 服务器端修复** (`server/server.js`):
在房主离开时添加 `players` 字段：
```javascript
if (isHost) {
  // 房主离开，删除房间
  broadcastToRoom(roomCode, {
    type: 'opponent_left',
    message: '房主已离开房间',
    players: room.players.map(p => p ? {
      time: p.time,
      moves: p.moves,
      undoLeft: p.undoLeft,
      color: p.color,
      name: p.name
    } : null)
  }, ws);
  rooms.delete(roomCode);
}
```

**2. 前端修复** (`frontend/src/App.vue`):
移除 `isPlaying` 条件限制，并根据身份清理状态：
```javascript
case 'opponent_left':
  showToast(data.message || '对手已离开房间');

  // 无论 isPlaying 状态如何，都处理对手离开消息
  if (gameMode.value === 'online') {
    // 清空游戏状态，重置为等待状态
    gameStore.initBoard(gameStore.boardSize);
    gameStore.currentPlayer = 1;
    gameStore.moveHistory = [];
    gameStore.winningLine = [];
    gameStore.opponentName = '';
    gameStore.isPlaying = false;

    // 从服务器消息中同步房主状态
    if (data.players) {
      data.players.forEach((p: any) => {
        if (p) {
          gameStore.players[p.color].time = p.time;
          gameStore.players[p.color].moves = p.moves;
          gameStore.players[p.color].undoLeft = p.undoLeft;
          gameStore.players[p.color].name = p.name;
        }
      });
    }

    // 如果是房主（对手是加入者），显示等待面板
    if (gameStore.isHost) {
      panels.value.opponentLeft = true;
    } else {
      // 如果是加入者（对手是房主），房间已删除，清理所有状态
      wsStore.clearRoomInfo();
      gameStore.roomCode = '';
      gameStore.gameMode = 'local';
      currentScreen.value = 'menu';
    }
  }
  break;
```

**涉及文件**:
- `server/server.js`
- `frontend/src/App.vue`

**验证方法**:
1. 房主刷新 → 玩家退出 → 房主可以正常创建房间
2. 玩家刷新 → 房主退出 → 玩家可以正常创建房间
3. 双方都刷新 → 任一方退出 → 可以正常创建房间

---

## 问题解决方案总结（最终版本）

### 核心问题修复（19个）

1. ✅ WebSocket循环引用 - 避免发送 `ws` 属性
2. ✅ WebSocket代理配置 - Vite/nginx 代理
3. ✅ Emoji重复显示 - 根据 `playerColor` 判断
4. ✅ 房间销毁逻辑 - 只有房主离开时删除
5. ✅ 重连识别 - 使用 `userId` 而不是 `playerName`
6. ✅ 页面刷新重连 - 保存和恢复 `gameMode`
7. ✅ 重连超时处理 - 清理定时器
8. ✅ 重连失败提示 - 友好的用户提示
9. ✅ 房间不存在处理 - 清理localStorage并返回菜单
10. ✅ 棋盘初始化错误 - 正确初始化二维数组
11. ✅ 游戏状态未完全重置 - 完全清空所有游戏状态
12. ✅ createEmptyBoard方法不存在 - 使用正确的 initBoard 方法
13. ✅ 重连后无法进入游戏 - 添加关键状态设置
14. ✅ 前端开发服务器崩溃 - 简化代理配置
15. ✅ 移动端设备无法创建房间 - 清理 LocalStorage
16. ✅ 对手退出提示不明显 - 创建 OpponentLeftPanel 组件
17. ✅ 对手退出后状态未更新 - 同步服务器状态
18. ✅ **重连消息处理器未注册** - 在 autoReconnect 前调用 setupWSMessageHandler
19. ✅ **对手退出后状态清理不完整** - 添加 players 字段并清理 LocalStorage

### 用户体验优化（3个）

20. ✅ UI组件统一样式 - 认输/退出确认弹框
21. ✅ 复制链接兼容性 - Clipboard API 降级方案
22. ✅ 对手退出弹窗 - 明显的提示面板

### 数据一致性（3个）

23. ✅ 前后端状态统一 - userId 匹配
24. ✅ localStorage 完整性 - 保存 gameMode
25. ✅ 房主状态同步 - 从服务器消息中同步

### 边界情况处理（3个）

26. ✅ 重连超时后重连 - 状态正确恢复
27. ✅ 房间已删除重连 - 自动返回菜单
28. ✅ 重连失败后清理 - 避免 LocalStorage 污染

---

**总计修复问题**: 28个  
**维护者**: iFlow CLI

---

## 2026-04-10 本次修复总结

### 修复的问题

本次修复了 2 个在线对战相关的核心问题，都是关于重连和状态管理的边界情况：

1. **问题28: 房主/玩家刷新后无法进入对局页面**
   - 根本原因：在 `onMounted` 中调用 `autoReconnect()` 前没有注册消息处理器
   - 解决方案：在 `autoReconnect()` 前先调用 `setupWSMessageHandler()`
   - 影响范围：所有刷新页面后重连的场景
   - 修复文件：`frontend/src/App.vue`

2. **问题29: 一方刷新后，对方退出房间无法创建房间**
   - 根本原因：
     - 房主离开时 `opponent_left` 消息缺少 `players` 字段
     - 前端收到消息后没有清理 LocalStorage
     - `opponent_left` 处理只在 `isPlaying` 时生效
   - 解决方案：
     - 服务器端添加 `players` 字段
     - 前端移除 `isPlaying` 条件限制
     - 根据身份清理状态和 LocalStorage
   - 影响范围：一方刷新后，对方退出房间的场景
   - 修复文件：`server/server.js`、`frontend/src/App.vue`

### 修复效果

✅ 房主刷新 → 成功进入对局页面  
✅ 玩家刷新 → 成功进入对局页面  
✅ 房主刷新后玩家退出 → 可以正常创建房间  
✅ 玩家刷新后房主退出 → 可以正常创建房间  
✅ 双方都刷新后任一方退出 → 可以正常创建房间  

### 技术要点

1. **消息处理器注册时机**
   - 必须在发送请求之前注册消息处理器
   - 否则无法接收到服务器的响应

2. **Computed 属性的只读特性**
   - `computed` 属性默认是只读的
   - 不能直接赋值，必须修改原始数据源
   - 在本项目通过 `gameStore.gameMode` 修改

3. **状态清理的完整性**
   - 根据用户身份决定清理策略
   - 房主：显示等待面板，保留房间
   - 加入者：房间已删除，清理所有状态
   - 必须清理 LocalStorage 避免状态污染

### 涉及的文档更新

1. **AGENTS.md**
   - 添加"开发环境"部分
   - 记录 Node.js 运行基础条件
   - 添加进程管理说明
   - 添加 AI 启动前后端进程的方法

2. **问题记录与解决方案.md**
   - 添加问题28和问题29的详细记录
   - 更新修复问题总数为 28 个

### 验证方法

1. **刷新重连测试**
   - 房主创建房间，玩家加入
   - 房主刷新页面 → 验证能进入对局
   - 玩家刷新页面 → 验证能进入对局
   - 双方都刷新 → 验证都能进入对局

2. **退出后创建房间测试**
   - 房主刷新 → 玩家退出 → 验证房主能创建房间
   - 玩家刷新 → 房主退出 → 验证玩家能创建房间
   - 双方都刷新 → 任一方退出 → 验证能创建房间

---

**修复日期**: 2026-04-10  
**修复人员**: iFlow CLI  
**修复耗时**: 约 30 分钟