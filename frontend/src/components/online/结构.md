frontend/
├── index.html                    # 入口 HTML（标题已更新为"五子棋在线对战"）
├── vite.config.ts                # Vite 配置（添加了 @ 路径别名）
├── tsconfig.app.json             # TypeScript 配置（添加了路径映射）
├── package.json                  # 依赖（新增 @vue/tsconfig）
├── public/
│   ├── ai.js                     # AI 引擎（从根目录复制）
│   ├── ai-worker.js              # Web Worker（从根目录复制）
│   └── ...
└── src/
    ├── main.ts                   # 入口文件
    ├── style.css                 # 游戏主题样式（替换了 Vite 默认样式）
    ├── App.vue                   # 主组件（完整游戏流程编排）
    ├── types/game.ts             # 完整类型定义
    ├── stores/
    │   ├── game.ts               # 游戏状态管理（含落子、胜负判断、悔棋、计时、导出等）
    │   └── websocket.ts          # WebSocket 状态管理（房间管理、消息处理）
    ├── composables/
    │   └── useAI.ts              # AI 组合式函数（Web Worker 集成）
    └── components/
        ├── menu/MenuScreen.vue   # 主菜单
        ├── game/
        │   ├── GameBoard.vue     # SVG 棋盘渲染
        │   ├── PlayerInfo.vue    # 玩家信息
        │   └── GameControls.vue  # 游戏控制按钮
        ├── online/
        │   ├── LocalSetupPanel.vue   # 本地对战设置
        │   ├── AISetupPanel.vue      # 人机对战设置
        │   ├── OnlinePanel.vue       # 在线对战面板
        │   ├── CreateRoomPanel.vue   # 创建房间
        │   ├── WaitingPanel.vue      # 等待对手
        │   └── PasswordPanel.vue     # 密码输入
        ├── RulesPanel.vue        # 游戏规则
        ├── HistoryPanel.vue      # 历史记录
        ├── WinModal.vue          # 胜利弹窗
        ├── UndoRequestModal.vue  # 悔棋请求弹窗
        └── Toast.vue             # 消息提示